<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Semi-transparent teal background (glassy look) -->
    <solid android:color="#AA006666" />

    <!-- Rounded corners -->
    <corners android:radius="20dp" />

    <!-- Inner padding so content doesn’t touch edges -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" />

    <!-- Optional: subtle drop shadow for depth -->
    <size android:width="300dp" />
</shape>
